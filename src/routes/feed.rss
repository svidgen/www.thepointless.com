<!-- DEPRECATED: src/routes/feed.rss

This file was replaced by src/ssg/feed.rss.ts which generates the RSS feed
at build time. The new generator imports src/news.cjs and produces the same
feed content (with proper XML escaping and a description fallback).

If you want to remove this legacy template file entirely, delete it from
the repository. It is left here as a reference for now.
-->
