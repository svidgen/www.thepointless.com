import {
	DomClass, setType, isa, getNodes,
	MouseCoords, NodeBox,
	MainLoop,
	on, onready,
	trackEvent,
	InstallLink,
	Game
} from '../../../../lib/shooty-ship/game.cjs';

export function onload() {
	const x = [DomClass];
}